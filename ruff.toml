# Configuración de ruff (linter). Ver https://docs.astral.sh/ruff/
line-length = 120
target-version = "py311"
extend-exclude = ["notebooks"]

[lint]
# Default de ruff (errores de import/sintaxis + pyflakes) + orden de imports (isort).
select = ["E4", "E7", "E9", "F", "I"]
