# Dependencias del deploy (Streamlit Cloud) y del pipeline (GitHub Actions).
# Rangos modernos a propósito: pines viejos (numpy 1.26 / streamlit 1.35) rompían
# el build en el Python nuevo de Cloud. numpy lo trae pandas; requests/dotenv no
# se usan (los scrapers usan urllib de la stdlib).
pandas>=2.2
streamlit>=1.58
plotly>=5.24
