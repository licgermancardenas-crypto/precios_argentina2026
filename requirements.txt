# Scraping (API HTTP pura — no necesita Playwright)
# playwright==1.44.0  # solo si en el futuro alguna cadena requiere browser

# Data
pandas>=2.2
numpy>=1.26

# Base de datos
# sqlite3 viene incluido en Python stdlib

# Dashboard  (rangos modernos: el dashboard se desarrolla/testea contra Streamlit
# 1.5x. Pines viejos rompían el deploy; y `use_container_width` fue removido en
# streamlit reciente, por eso se migró a width="stretch" — necesita >=1.58.)
streamlit>=1.58
plotly>=5.24

# ML (v3 — se activa en esa fase)
# prophet==1.1.5
# scikit-learn==1.5.0

# Utils
python-dotenv>=1.0
requests>=2.32
