# Dependencias de desarrollo/tests.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest            # corre tests/
# Los tests usan unittest.TestCase (stdlib) y pytest los descubre y corre igual.
pytest>=8.0
# Linter / formato
ruff>=0.6
# Notebooks de análisis (notebooks/)
matplotlib>=3.7
jupyter>=1.0
